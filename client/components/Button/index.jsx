import { StyledWrapper } from "../Forms"

const Button = () => {
  return (
    <StyledWrapper>
      <button className="submitButton">Submit</button>
    </StyledWrapper>
  )
}

export default Button